# 2026-07-07 — projectome_analysis

## [meta] session — 10:19

## [ops] 5µm widefield SMB

- User updated **252385** widefield resample and moved **251637** + **252385** into:
  `\\10.102.8.200\microscopy_data\fMOST\5micron_datasets\{sample}-CH1_resample\resample_5um`
- Verified on SMB: 251637 = 20744 slices; 252385 = 20788 slices
- Code: `Visual_toolkit.py` — `LOW_RES_SHARE_ROOT` + paths for 251637, 252385
- Manifest: `252385` `5_micron_data_copied=Yes`; 251637 note updated (moved, still Yes)
- Data progress rebuild pending (needs conda `projectome` / matplotlib)

## [ops] bulk visual 252385 INS

- 29/29 widefield PNGs OK (step1 INS group); prior output on old `W:\fMOST\visual\...`
- Output root switched to `\\10.102.8.200\microscopy_data\fMOST\visual\` in `bulk_visual_multi_monkey.py`
- Remaining: 18 combined PrCO-rescue keepers (`053`–`072`) — not yet rendered

---

## [meta] session — 11:51 (Visual_toolkit hires revert + imagecodecs)

## [scope] 相关代码

- `main_scripts/Visual_toolkit.py` — `_read_tiff`, `_download_http_block`, `plot_soma_block`, `get_high_res_block`
- `main_scripts/Visual_toolkit_gui.py` — same toolkit; GUI vs bulk share one code path
- `group_analysis/scripts/run_bulk_visual_sample.py`, `bulk_visual_multi_monkey.py`
- `main_scripts/step3.1.bulk_visual_data.py` — reference bulk (`GRID_RADIUS=2`)

## [ops] 本轮改动

- User reported **bad hires (soma) plots** in bulk **and** GUI → root cause was **TIFF read**, not separate bulk/GUI logic.
- **Reverted** today’s experimental fixes to **pre-break (ae05802-style) behavior:**
  - `plot_soma_block()` → **MIP** (`np.max(volume_3d, axis=0)`), not soma Z-slice
  - `_read_tiff()` → tifffile then single-page PIL fallback (removed multi-page PIL stack + `_normalize_http_cube`)
  - Bulk `grid_radius` default → **1** (matches GUI)
- **Kept:** `LOW_RES_SHARE_ROOT` / `5micron_datasets` SMB paths; UNC visual output root
- **Installed:** `imagecodecs==2025.3.30` in conda env **`projectome`** (required for LZW multi-page HTTP cubes)

## [decision] Visual_toolkit hires — **check these keys first** (next session playbook)

When user asks about wrong hires plots, low contrast, GUI vs bulk differences, grid size, or “why did it look different before?” — **run this checklist before changing plot code:**

| Key question | What to check | Expected / note |
|--------------|---------------|-----------------|
| **1. TIFF stack shape** | `python -c "import importlib.util; print('imagecodecs', bool(importlib.util.find_spec('imagecodecs')))"` in **`projectome`** env | Must be **True**. Without `imagecodecs`, `tifffile` fails → PIL reads **page 1 only** → cube **360×360** instead of **(90, 360, 360)** → garbage MIP/soma |
| **2. One cube smoke test** | Download/cache one HTTP cube; `tifffile.imread(path).shape` | **`(90, 360, 360)`** per cube |
| **3. Plot projection** | `plot_soma_block()` in `Visual_toolkit.py` | Canonical = **MIP** on Z; soma-slice was a reverted experiment |
| **4. grid_radius** | `get_high_res_block(..., grid_radius=)` | **1** = 1 cube (~234×234×270 µm); **2** = 3×3×3 (~702×702×810 µm). GUI default **1**; von Economo `step3.1` bulk used **2** |
| **5. Contrast** | `_save_plot` / percentile block | **0.5–99.5%** + **gamma 0.5** — **identical** in GUI and bulk |
| **6. Data sources** | Low-res vs high-res paths | Low-res: `\\10.102.8.200\...\5micron_datasets\{sample}-CH1_resample\resample_5um`; High-res: HTTP `bap.cebsit.ac.cn/monkeydata/{sample}/cube/...` |
| **7. Output root** | Bulk scripts `PARENT_OUT` | UNC `\\10.102.8.200\microscopy_data\fMOST\visual\` — **not** remapped `W:\fMOST` |
| **8. “Older look”** | Compare MIP vs Z-slice, grid_radius FOV, cached corrupt cubes | Brighter/softer MIP vs single slice; wider FOV changes histogram; stale **360×360** cache → delete cube cache and re-fetch |

**Do not** assume bulk and GUI diverge — both import `Visual_toolkit`. Fix env/read path first; only then tune `grid_radius` or re-run bulk.

## [verify]

- `imagecodecs` installed: **2025.3.30** (`projectome` env)
- Code state: MIP + ae05802-style `_read_tiff` + UNC paths
- Example bulk re-run (soma): `run_bulk_visual_sample.py --sample 251637 --with-soma --soma-only --groups INS`

## [next] 下一步

1. Re-run hires soma for **252385 INS+PrCO** (and spot-check **251637_007**) with MIP + imagecodecs
2. Optionally use `--grid-radius 2` to match von Economo `step3.1` if user wants wider FOV
3. Rebuild data progress when convenient

---

## [meta] session — 12:02 (252385 full visual rerun)

## [ops] Bulk visual 252385 — clean rerun

- Removed prior `visual/252385/cube_data_*` outputs (20260707, hires, legacy PrCO mix)
- Running: `--groups INS,PrCO --with-soma --grid-radius 2 --run-stamp 20260707` (hi+lo res, PNG+NIfTI, step1 tables)
- Output: `\\10.102.8.200\microscopy_data\fMOST\visual\252385\cube_data_252385_{INS|PrCO}_20260707\`

## [ops] Repo sync (pending commit)

- `Visual_toolkit.py`: 5µm SMB paths + MIP hires (reverted from broken Z-slice experiment)
- `bulk_visual_multi_monkey.py`: UNC visual root
- `run_bulk_visual_sample.py`: new per-sample runner (step1 table export, soma-only NIfTI fix)
- `dataset_status_manifest.csv`: 252385/251637 5µm notes
- Journal: hires diagnostic checklist in CURRENT.md
