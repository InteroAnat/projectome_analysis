# Daily — 2026-07-01

## [meta] 会话 — repo cleanup + data progress track

- **分支:** `cleaner_projectome_scripts`
- **操作类型:** 重构 / 文档 / 数据分析 tooling

## [scope] 针对范围

- `group_analysis/data_progress/` — manifest → table → plot → track
- `group_analysis/scripts/build_data_progress.py`, `plot_dataset_status_overview.py`
- `group_analysis/docs/dataset_status_manifest.csv`, `dataset_status_overview.md`
- `main_scripts/` cleanup（examples、gold/fib、old step3.2 备份；保留 `step3.2.run_brain_viz_meshRender.py` 重命名）
- `.gitignore` — root clutter / sqlite / swp
- Git：`safe.directory=D:/projectome_analysis`（SSD 迁移后 dubious ownership 绕过）

## [ops] 完成

### Repo cleanup（`cleaner_projectome_scripts`）

- 删除冗余脚本：`learn.py`, `quickstart.py`, `example_*`, `tempvis_*`, `fid_gold.py`, `fibonacci_toolkit.py`, `old.*` 备份等
- 删除 superseded step1 输出目录（Apr27 252383/252385、旧 251637 results）、step1 logs、`gou_julia_scripts/monkeytemp/`
- 删除 root clutter（tree/t.html/worker/joined_fnt 等）及 8 张 legacy `dataset_status_*.png`
- **保留** brain viz：`step3.2.run_brain_viz_meshRender.py.py` → `step3.2.run_brain_viz_meshRender.py`

### Data progress 模块

- 单一输出：`data_progress_table_YYYYMMDD_HHMM.csv` + 同名 PNG（含 TOTAL 行、`generated_at`）
- 移除 sidecar：`data_progress_insula_distribution.csv`、`data_progress_totals.csv`
- 修复 `pipeline_stage_code` / `pipeline_stage`（顺序阶段，next action；251637 走 ref-curated step1）
- 图：去掉 Pipeline 列；列组 shading；标题带 timestamp
- 每次 build 自动清理 unstamped 旧文件 + legacy `dataset_status_*.png`

### Git / Cursor

- 文件系统 owner 仍为旧 SID；全局 `git config safe.directory` 已生效，终端 git 正常
- Cursor 分支 UI 若仍空 → Reload Window 或管理员 `takeown` 根治

## [verify]

```powershell
python group_analysis/scripts/build_data_progress.py
# → data_progress_table_20260701_1416.csv + .png
```

-  cohort：insula 353/2400；ION 1205/1225；combined 353；5µm 1/8

## [next]

1. Push `cleaner_projectome_scripts` → GitHub（本 commit）
2. 可选：管理员 `takeown` 去掉 `safe.directory` 依赖
3. 797/252790：ION API 仍空，需上传或更新 manifest
